import { css } from '@emotion/react';

const lottoAnswerInputContainerStyle = css({
  display: 'flex',
  justifyContent: 'space-between',
});

const lottoAnswerInputStyle = css({
  display: 'flex',
  gap: '8px',
});

const bonusNumberInputStyle = css({
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'end',
});

export { lottoAnswerInputContainerStyle, lottoAnswerInputStyle, bonusNumberInputStyle };
