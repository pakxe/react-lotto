import { css } from '@emotion/react';

const layoutContainerStyle = css({
  width: '100%',
  minHeight: '100vh',

  display: 'flex',
  flexDirection: 'column',
  justifyContent: 'center',
  alignItems: 'center',
});

export { layoutContainerStyle };
