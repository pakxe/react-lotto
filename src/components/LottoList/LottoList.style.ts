import { css } from '@emotion/react';

const lottoListStyle = css({
  display: 'flex',
  flexDirection: 'column',
  gap: '8px',

  maxHeight: '250px',
  overflowY: 'scroll',
});

export { lottoListStyle };
